# Architecture decision records

One file per decision that shaped this loader, in the core's ADR format:
context, options, decision, consequences. Change a decision by adding a
new record that supersedes the old one.

```{toctree}
:maxdepth: 1

0001-refuse-numeric-iban-cells
0002-one-sheet-read-never-run
```
