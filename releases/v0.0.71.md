# pain001-loader-xlsx v0.0.71

This release aligns `pain001-loader-xlsx` with `pain001` core v0.0.71.

## What's Changed

- **Resource Cleanup**: Close openpyxl workbook resources on malformed input and early iterator termination, as well as upon completed reads (pain001#180).
- **Documentation & Invariants**: Generate the canonical README, enforce drift checks in CI, and update repository guidance and invariants.
- **Ecosystem Coordination**: Synchronize version 0.0.71 across the pain001 ecosystem while preserving the structural plugin contract (`pain001 >= 0.0.56, < 1`).

## Upgrade

```sh
pip install --upgrade pain001-loader-xlsx
```

Nothing to migrate.
